namespace Course
{
    struct Point
    {
        //Atributos da struct
        public double X, Y;

        public override string ToString()
        {
            return "(" + X + "," + Y + ")";
        }
    }
}